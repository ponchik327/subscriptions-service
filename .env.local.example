# Local development overrides — copy to .env.local and adjust as needed.
# These values are used by `make run`, `make migrate-up`, and `make migrate-down`.
# They are NOT used by docker compose (docker compose reads .env).
POSTGRES_DSN=postgres://app:app@localhost:5432/subscriptions?sslmode=disable
POSTGRES_MIGRATIONS_PATH=file://migrations
